**主要技术**

1. **Python 3.10**
   - 负责程序组织、检测逻辑、配置读写和桌面入口。
   - 当前入口为 [main.py](vscode-webview://1vt3n3qgd5imiefn406j4ps9bu9oco235e1eu8d518fa8lpnetn0/main.py)。
2. **PySide6 6.11.1 / Qt**
   - 构建桌面界面。
   - 使用 `QMainWindow`、`QWidget`、`QSplitter`、`QScrollArea`、`QTableWidget` 等控件。
   - 使用 Qt 信号槽连接视频画面、控制面板和后台线程。
   - 使用 `QThread` 避免模型推理阻塞界面。
3. **OpenCV**
   - 通过 `cv2.VideoCapture` 读取摄像头或视频文件。
   - 负责 BGR/RGB 转换、检测框绘制、多边形包含判断和报警截图保存。
   - 视频读取封装在 [video_source.py](vscode-webview://1vt3n3qgd5imiefn406j4ps9bu9oco235e1eu8d518fa8lpnetn0/video_source.py)。
4. **Ultralytics YOLO**
   - 使用本地 [yolo11n.pt](vscode-webview://1vt3n3qgd5imiefn406j4ps9bu9oco235e1eu8d518fa8lpnetn0/yolo11n.pt) 权重。
   - 推理时限制为 COCO 类别 `0`，即只检测行人。
   - 模型调用和结果处理位于 [detection_engine.py](vscode-webview://1vt3n3qgd5imiefn406j4ps9bu9oco235e1eu8d518fa8lpnetn0/detection_engine.py)。
5. **Supervision ByteTrack**
   - 将每帧检测结果关联为持续的目标 ID。
   - 同一行人在连续帧中保持相对稳定的 `track_id`。
   - 当前日志提示 `ByteTrack` API 将在未来 Supervision 版本移除，后续升级依赖时需要迁移。
6. **NumPy**
   - 视频帧、检测框和多边形坐标使用 NumPy 数组处理。
   - 作为 OpenCV、YOLO 和 Supervision 之间的图像数据载体。
7. **JSON / JSONL**
   - [config_store.py](vscode-webview://1vt3n3qgd5imiefn406j4ps9bu9oco235e1eu8d518fa8lpnetn0/config_store.py) 使用 JSON 保存视频源、播放速度和警戒区域。
   - [alarm_service.py](vscode-webview://1vt3n3qgd5imiefn406j4ps9bu9oco235e1eu8d518fa8lpnetn0/alarm_service.py) 使用 JSONL 逐行保存报警事件。
   - 每次报警还会写入 JPEG 截图。
8. **Windows `winsound`**
   - 报警时异步播放蜂鸣声。
   - 声音播放失败不会中断检测线程。

**整体构成**



```text
main.py
  |
  v
MainWindow
  |-- SourcePanel       视频源选择
  |-- ZonePanel         区域管理
  |-- PlaybackPanel     文件播放控制
  |-- EventPanel        报警记录
  |-- VideoWidget       视频和多边形交互
  |
  v
DetectionWorker (QThread)
  |-- VideoSource       摄像头/文件读取
  |-- DetectionEngine   YOLO + ByteTrack + 区域判断
  |-- EventStore        截图和事件持久化
  `-- AlarmPlayer       声音报警
```

**检测流程**

1. `VideoSource` 从摄像头或文件读取 BGR 视频帧。
2. YOLO 检测类别为 `person` 的目标。
3. Supervision 将检测结果送入 ByteTrack，生成目标 ID。
4. 程序取行人框底边中心点作为落地区域判断点。
5. 使用 `cv2.pointPolygonTest` 判断该点是否位于闭合警戒区域内。
6. 使用 `(区域名称, 目标ID)` 记录进入时间。
7. 停留时间达到阈值后产生报警。
8. 冷却时间内不会对同一目标和区域重复报警。冷却记录按 `(区域名称, 目标ID)` 保存，
   且比会话本身活得更久 —— 要压住的正是「离开又立刻回来」产生的下一个会话。被压住
   的会话不算已报警，目标一直不走的话，冷却期一过仍会补报一次。
9. **轨迹短暂消失不立即结算会话**（`SESSION_GAP_TOLERANCE_SECONDS`，1.5 秒）。半身被
   遮挡的人在低功耗预设（每 4 帧检测 + INT8）下检测会断断续续，而"一帧没检到就关闭
   会话"会让下一个会话从零开始计时 —— 表现就是"人一直在区域里，停留时间却一直刷新，
   一条报警都没有"。容忍期内计时继续（按进区时刻算的墙钟时长），超过才结算；真正
   离开又回来的情况仍由冷却时间兜住。
10. 配套的两个检测侧旋钮（都是为了扛遮挡）：
    - 检测置信度下限 `DETECTION_CONFIDENCE = 0.1`（ultralytics 默认 0.25）。被遮挡的人
      置信度常掉到 0.1~0.2，默认阈值下这些检测会被直接丢掉，ByteTrack 也就没机会用它
      维持已有轨迹 —— 它的第二段关联本来就是专门吃低分检测来扛遮挡的。放低不会凭空
      多出新轨迹：`track_activation_threshold` 仍是 0.25，低分检测只能延续已有轨迹。
    - `lost_track_buffer` 提到 60 帧（≈2 秒），要 ≥ 上面的会话容忍时间，否则轨迹先被
      追踪器丢掉、人再出现时拿到新 ID，会话照样重新计时，容忍就形同虚设。
    **这两个旋钮的效果还没有被量化**（深度自检统计的是会话与告警次数，不是召回率）；
    要判断它们值不值得，需要那份带标注的真实素材评测。
11. 检测框、ID 和区域状态绘制到视频帧。
12. 报警事件保存到 JSONL、截图写入磁盘并更新界面表格。

**视频源中断**

- 视频文件读到结尾属于正常结束：开了循环播放就从头再来，否则停止检测。
- 摄像头或网络流中断则一直重连，不会放弃。等待时间按 1、2、4、8、16、30 秒指数退避，
  上限 30 秒；只有真的重新读到帧才会把退避重置回 1 秒（RTSP 地址常常能打开却一帧不来）。
- 退避用的是可中断的等待，按下停止会立刻退出，不会让界面等满整个退避时间。

**画面帧与检测帧**

- 检测每帧都跑，**显示**则允许丢帧。界面跟不上推理速度时（低功耗预设能跑到 100 fps，
  而界面每帧要做一次颜色转换和缩放绘制），worker 会把这一帧丢掉而不是排队。
  Qt 的跨线程信号既不合并也不丢弃，1080p 一帧 BGR 就是 6 MB，不丢帧的话队列会按秒
  堆起来，先表现为画面越来越滞后，最后撑爆内存。
- 界面处理完一帧会回执一次（`frame_consumed`），worker 据此决定下一帧发不发。
- 只丢显示不丢检测：跳过检测会让驻留计时和告警判定失真。标注帧也照画，因为同一帧
  还要用作告警取证截图 —— 省掉绘制会出现没有画框的取证图。

**配置与取证数据的容错**

- `config.json` 与 `profiles/*.json` 是纯文本，用户能手改。读取时逐字段兜底：坏字段
  只回落它自己（并写一条 warning 说明是哪个字段），**不会**连累其它字段 —— 尤其不会
  让用户的警戒区域一起消失。
- 多边形顶点只保留真正的「数值二元组」：`cv2.pointPolygonTest` 对形状很挑，一个只有
  1 个坐标的点会让它抛断言错误、把检测线程带停。坏顶点被丢掉，剩下的照用；少于三个
  顶点时一律视为未闭合。
- 整份文件解析不了（不是 JSON、根节点不是对象）时，把它改名成
  `config.json.bad-<时间戳>` 留档，再用默认配置启动 —— 既不因为一个坏字符让程序每次
  启动都失败，也不覆盖掉用户可能想手工找回的区域坐标。
- 报警记录的读取跳过坏行（不是 JSON、或 JSON 但不是对象），一行坏数据不该让整份记录
  打不开。「清空记录」按运行模式过滤时，读-改-写全程持锁，不吞掉检测线程刚好写下的
  那一条。

**取证留存**

- 详情弹窗里的截图是缩放到 330×300 的预览，**双击用系统默认程序打开原图**
  （`QDesktopServices`，Windows 上就是默认图片查看器）。取证要看的是原图 —— 目标手里
  拿的什么、区域边界压在哪，缩略图看不出来。图片不可用时不给这个入口（点了没反应比没有
  入口更让人困惑），打开失败会明确报错。
- 设置页的「取证留存」里有一个**显式开关，默认关闭**：不勾选就绝不自动删除任何文件。
  自动清理与「立即清理」都受这一个开关约束（关着的时候数值框与按钮都是灰的），所以
  用户不必去猜某个按钮会不会真的动手。程序第一版把这个功能默认开着，等于在用户还没
  看过设置的时候就动他的取证材料 —— 这类功能必须由人显式打开。
- 打开后按两条独立规则从最旧的截图开始清理：截图保留天数（默认填 15 天）与容量上限
  （默认填 2048 MB）。任一为 0 表示那条规则不生效，两条都为 0 就什么都不删。
- **只清理截图，报警记录一直保留**。两者体量差了三个数量级：本地实测 220 张截图
  102 MB，对应的 `alarm_events.jsonl` 只有 700 KB —— 记录是纯文本索引（表格、详情、
  事后追溯都靠它），截图才是会把磁盘吃干净的那部分。更早的记录仍会显示在表格里，
  只是详情里的图会缺失。
- 清理在启动时与之后每小时各跑一次（`retention.py`）。配置刚从坏文件回落时不自动
  清理：那份默认值不代表用户的意愿。
- 旧配置（只有天数/上限、没有开关）一律按**未启用**处理，只记一行日志说明 —— 那些
  数值是旧版本自己写进去的默认值，用户从没点过头，不能据此推断他同意删材料。
- 只动 `events/screenshots/` 下的 jpg/jpeg/png，目录里的其它文件一律不碰 —— 自动
  清理最忌讳删掉自己没生成过的东西。

**远程通知**

- 报警时可以 POST 一条 JSON 到指定地址（设置页「远程通知」，**默认关闭**）。本机蜂鸣
  解决不了这个产品的核心问题：没人守在现场时报警必须能离开这台机器 —— 而且取证材料
  同样不该只存在于被监控现场的那台机器上，机器被搬走、盘坏掉，证据就一起没了。
- 两种请求体：`通用 JSON`（`text` + `event` + 可选 `screenshot_base64`）与
  `企业微信 / 钉钉文本`（`{"msgtype":"text","text":{"content":…}}`，这类机器人只收
  文本，附不了图）。
- 发送在一条后台线程里做，界面与检测线程都不碰网络；待发队列有上限（32 条），断网时
  丢通知而不是堆内存 —— 报警记录与截图早已落盘，丢通知不会丢事实。失败重试 2 次，
  异常一律转成结果而不是抛出去（`notifications.py`）。
- 只在标准库里实现（`urllib`）：这个项目靠 PyInstaller 打包，多一个 requests 就多一份
  要跟着打进包、跟着升级排查的东西。
- 「开了但地址不可用」会明确报出来（状态栏 + 设置页红字）。这是最危险的失败：报警响了、
  记录写了，操作员以为通知也发了，实际什么都没出去。
- **视频模式（测试播放）里的报警默认不发通知**：拿一段视频试跑一遍，群机器人就会被刷
  一串「闯入报警」。本地蜂鸣被刷无所谓，往外部广播不一样。判据是事件自己的运行模式，
  不是当前配置的模式。要现场演示完整链路可在设置页打开「视频模式（测试播放）也发送」。

**版本、资源与构建的可复现性**

- 运行期版本号在 `app_version.py`，`execode/version_info.txt` 只在打包时被 PyInstaller
  读走。两者必须一致，`--selftest` 会比对（源码模式读 version_info.txt，打包版读 exe
  自己的版本资源）——「exe 属性写着 1.0.0、日志里写着 1.1.0」正是双份版本号最容易出的岔子。
- 启动时日志会记一行版本与运行上下文（版本、是否冻结、exe、程序目录、数据目录），
  随后再记一行**生效设置**（模式、视频源、设备、低功耗、模型、留存、通知、配置是否
  可信）。现场支持的第一个问题永远是「你装的是哪一版、配了什么」。
- `asset_manifest.json` 记录 `yolo11n.pt` 与 `warming_converted.wav` 的 sha256，
  `--selftest` 逐项校验。这两个文件被换掉、拷坏、解压截断时程序都照样加载，只是
  「检测不出人」或「报警没声音」，都不报错。**替换过发布资源就要重新生成清单**：
  打包版 `行人警戒区域监控.exe --write-asset-manifest`，源码树
  `python tools\write_asset_manifest.py`。生成逻辑只有一份（`asset_manifest.py`），
  两条入口共用 —— 现场没有 Python 环境、`tools/` 也不随发布包发布，所以这件事必须
  由 exe 自己能做，否则自检给出的修复建议是用户执行不了的。
- 依赖版本锁在 `constraints.txt`（v1.1.0 就是用那一组构建并验收的），
  `build.ps1` 与 CI 都按它安装。不钉版本的代价很具体：半年后重建同一个版本会装上那时的
  ultralytics/supervision，装出来的包与验收过的不是一回事。
- CI（`.github/workflows/tests.yml`）在 windows-latest 上跑 `ruff check` + `pytest`；
  本地等价的一条命令是 `.\run_tests.ps1`。

**Windows 上的编码约定**

这个程序只在 Windows 上跑，而 Windows 上的默认解码方式与 UTF-8 不一致，踩过两次坑，
所以统一成：

- **日志文件写 UTF-8 带 BOM**（`utf-8-sig`）。PowerShell 5.1 的 `Get-Content` 与旧版
  记事本默认按 ANSI 解码，无 BOM 的中文日志打开就是整片乱码 —— 排查现场问题时很容易
  被当成「程序在乱写日志」。
- **`.ps1` 脚本写 UTF-8 带 BOM**。PowerShell 5.1 读脚本时同样不认无 BOM 的 UTF-8，
  结果是中文注释把脚本语法解析搞崩（`build.ps1` / `package.ps1` 一直是这样存的）。
- **`config.json` / `profiles/*.json` / `alarm_events.jsonl` 写 UTF-8 不带 BOM**：
  它们由程序自己读写（`encoding="utf-8"`），也要能被其它工具当标准 JSON/JSONL 处理。

**画面渲染**

标注帧按「地面 → 行人 → 信息」三层合成，顺序决定了观感，不能随便换：

1. **警戒区先落在地上**（`detection_engine._draw_ground_zones`）。贴地感来自**透视
   一致性**：屏幕宽度恒定的线看起来贴在屏幕上，而地面上一段固定宽度的警戒带，靠近
   相机的部分看起来更宽 —— 所以边带宽度随 y 线性增长（`BAND_WIDTH_RATIO`，画面底部
   最宽）。层次是「很淡的面（`SOFT_ALPHA`）+ 实边带 + 上沿亮线 + 边带下方接地暗带」，
   一次 `addWeighted` 混完，不为每个元素各混一次。
2. **再把行人的像素贴回区域之上**（`_restore_people`）。这样边界看起来在人后面，
   而不是浮在人身上。只用检测框内接的椭圆近似人体轮廓 —— 上分割模型能拿到真实掩码，
   但推理会慢一倍左右、低功耗预设的 INT8 模型还要重新导出，而椭圆已经足够让边界
   「断在人身前」。只处理与区域包围盒有交集的框：没人靠近时这一步一分钱不花。
3. **最后画检测框与标签**，信息层永远在最上面。

控件（`video_widget._draw_zones`）**只画编辑用的那一层**，而且**只在用户真的可能编辑时
才画**（鼠标停在画面上、正在拖顶点、或区域还没闭合）：区域本身（贴地渲染、被行人遮挡）
由引擎烧进画面里，而编辑用的虚线轮廓是界面元素，只能画在图像之上 —— 一直显示的话就会
横穿在人身上，看着突兀。轮廓按顶点的**真实**位置画，只有顶点标记才用夹住后的位置
（见下）—— 两者混用会让边界画到错的地方去，界面上会出现两条对不上的边界。

其余细节：虚线用深色垫底 + 白色（保证浅色地面上也看得见，且一眼就是界面元素而不是画面
里的实体边界）；开了抗锯齿；顶点手柄只画激活区域的，不再每个区域都挂一串圆点。

顶点允许落在画面之外（换过不同分辨率的视频源、或者把顶点拖出去都会这样），所以：

- 抓取判定**先于**"点是否在画面内"的判断 —— 否则画面外的顶点既拖不动也删不掉，
  永远卡在画面外。只有新增顶点才要求落在画面内（点在黑边上不该凭空加点）。
- 拖动按同一套线性映射外推，允许把顶点拖出画面、也允许拖回来；离画面最多一个
  `OFF_FRAME_MARGIN`（5%），免得配置里出现天文数字。
- 顶点被外推到控件可视区之外时，**标记**会被夹到控件边缘并画成指向真实方向的三角
  （`_handle_position`），命中判定用同一个位置 —— 否则窗口不大时画面外一点点的顶点
  在屏幕上就已经出界，那就再也抓不回来了。轮廓不受这个夹取影响。

调浓淡宽窄改 `detection_engine` 里那几个常量即可；看效果不用开 GUI：

```text
python tools\render_preview.py --frame 12 --out preview
python tools\render_preview.py --zones "790,400,1010,400,1010,700,790,700"   # 指定区域凑画面
```

它把「引擎烧进帧里的图」与「控件上最终显示的样子」各存一张 PNG。渲染成本可以量：

```text
python tools\benchmark_cpu_profiles.py --trials 1 --render --zones "…"
```

1080p、15 个检测框、一个约 75 万像素的区域，区域渲染（含遮挡）约 2.5 ms/帧；区域不
与任何框重叠时约 0.9 ms。实时监控是 25/30 fps（帧预算 33 ms），这点开销不影响功耗。
标注里的大头其实是检测框与标签的绘制（一帧十几个框就要 2.8 ms 起），不是区域。

**取证记录的落盘与读取**

- 截图用 ``imencode`` + ``write_bytes`` 写，不用 ``cv2.imwrite``。裸 ``cv2.imwrite`` 在
  非 ASCII 路径下会**静默失败**（返回 False，文件根本没写）；程序里之所以一直没出事，
  是因为 ultralytics 导入时把 ``cv2.imwrite`` 换成了它自己的 Unicode 安全版本 ——
  也就是说「取证能不能落盘」一直在依赖第三方的猴子补丁。改成不依赖补丁的写法，并且
  失败时写 warning（原来是静默返回空串）。
- 记录里存的是**相对 events 目录**的路径（``screenshots/<会话ID>_entry.jpg``）。绿色版
  的说明书要求「整个文件夹一起拷贝」，而绝对路径一旦被拷到别处，所有旧记录都会变成
  「取证不可用」。读取时由 ``EventStore.resolve_screenshot`` 解析；**旧记录存的是绝对
  路径，解析器会按文件名到当前截图目录再找一次**，所以换过安装目录的旧记录也能恢复。
- 详情弹窗里截图显示不出来时会说清是哪一种：未触发报警（本就没有报警截图）／截图未
  写入（记录里没有路径）／文件缺失（可能已被留存策略清理或手动删除）。原来一律显示
  「不可用」，三种完全不同的情况看起来一模一样，于是「记录里经常出现报警取证不可用」
  成了一个说不清、也没法判断要不要处理的现象。
- 浅自检里有一项**真写一张 JPEG** 的探针（``_probe_image_write``）：它挡住的就是
  「写图失败但没有任何报错」这类故障 —— 一旦哪天第三方库不再打那个 Unicode 补丁，
  打包版里每一次取证截图都会写不进去，而界面上只显示「报警取证不可用」。

**模块职责**

- [models.py](vscode-webview://1vt3n3qgd5imiefn406j4ps9bu9oco235e1eu8d518fa8lpnetn0/models.py)：`ZoneDefinition`、`AppConfig`、`AlarmEvent` 数据模型。
- [config_store.py](vscode-webview://1vt3n3qgd5imiefn406j4ps9bu9oco235e1eu8d518fa8lpnetn0/config_store.py)：配置加载和保存。
- [video_source.py](vscode-webview://1vt3n3qgd5imiefn406j4ps9bu9oco235e1eu8d518fa8lpnetn0/video_source.py)：摄像头、视频文件、速度、循环和时间位置。
- [detection_engine.py](vscode-webview://1vt3n3qgd5imiefn406j4ps9bu9oco235e1eu8d518fa8lpnetn0/detection_engine.py)：YOLO、ByteTrack、区域停留判断和画面标注。
- [detection_worker.py](vscode-webview://1vt3n3qgd5imiefn406j4ps9bu9oco235e1eu8d518fa8lpnetn0/detection_worker.py)：后台检测线程和 Qt 信号。
- [alarm_service.py](vscode-webview://1vt3n3qgd5imiefn406j4ps9bu9oco235e1eu8d518fa8lpnetn0/alarm_service.py)：报警声音、JSONL 记录和截图。
- [notifications.py](vscode-webview://1vt3n3qgd5imiefn406j4ps9bu9oco235e1eu8d518fa8lpnetn0/notifications.py)：报警的远程通知（拼请求体、重试、后台发送队列）。
- [retention.py](vscode-webview://1vt3n3qgd5imiefn406j4ps9bu9oco235e1eu8d518fa8lpnetn0/retention.py)：取证截图的留存策略（按天数与容量上限从最旧的开始清理）。
- [settings_binding.py](vscode-webview://1vt3n3qgd5imiefn406j4ps9bu9oco235e1eu8d518fa8lpnetn0/settings_binding.py)：配置字段与控件的声明式绑定（加一个设置只动字段和绑定表两处）。
- [app_version.py](vscode-webview://1vt3n3qgd5imiefn406j4ps9bu9oco235e1eu8d518fa8lpnetn0/app_version.py)：运行期版本号（与 execode/version_info.txt 由自检比对）。
- [security_monitor.py](vscode-webview://1vt3n3qgd5imiefn406j4ps9bu9oco235e1eu8d518fa8lpnetn0/security_monitor.py)：早期的最小示例，不参与桌面程序、不进发布包，仅供试验新思路。
- [video_widget.py](vscode-webview://1vt3n3qgd5imiefn406j4ps9bu9oco235e1eu8d518fa8lpnetn0/video_widget.py)：视频显示、坐标换算和多边形编辑。
- [main_window.py](vscode-webview://1vt3n3qgd5imiefn406j4ps9bu9oco235e1eu8d518fa8lpnetn0/main_window.py)：整体布局、控件状态、配置同步和线程生命周期。
- [main.py](vscode-webview://1vt3n3qgd5imiefn406j4ps9bu9oco235e1eu8d518fa8lpnetn0/main.py)：应用启动和依赖错误提示。
- [requirements.txt](vscode-webview://1vt3n3qgd5imiefn406j4ps9bu9oco235e1eu8d518fa8lpnetn0/requirements.txt)：第三方 Python 依赖。

**运行测试**

测试依赖装在 `requirements-dev.txt` 里（运行程序本身不需要）：

```text
pip install -r requirements.txt -r requirements-dev.txt -c constraints.txt
```

最省事的是直接跑脚本（静态检查 + 测试）：

```text
.\run_tests.ps1
```

两种跑法都可以，任选其一：

```text
pytest -q
python -m unittest discover -s tests -t . -q
```

本机可用的解释器是 `.venv-openvino\Scripts\python.exe`（项目根没有 `.venv`；
`.venv-build` 是 PyInstaller 打包环境，里面没装 pytest，别拿它跑测试）。

`tests/` 是一个包（有 `__init__.py`），`pytest.ini` 里把项目根加进了 `sys.path`；
测试用的是顶层导入（`from detection_engine import DetectionEngine`），所以不要在
`tests/` 目录里面直接跑。界面测试用 `QT_QPA_PLATFORM=offscreen`，构造 `MainWindow`
的测试必须把 `main_window.CONFIG_PATH` / `EVENTS_DIR` / `PROFILES_DIR` patch 到临时
目录，否则会碰开发机上真实的配置与取证数据。

静态检查用 `ruff.toml`（不是 pyproject.toml，理由同 `pytest.ini`）。规则集刻意收窄，
只开能抓真 bug 的那几类，详见该文件里的说明。

部署或分发前需要按最终锁定版本核对 PySide6、Ultralytics、OpenCV、Supervision 和 NumPy 的许可证，尤其是 Ultralytics 的开源与商业授权条件。