# pytest 配置。用 pytest.ini 而不是 pyproject.toml，是为了不和打包工具链
# 产生任何牵连（本项目用 PyInstaller 发布，不做 pip 安装）。
[pytest]
testpaths = tests
# 测试用顶层导入（from detection_engine import ...），项目根必须在 sys.path 上。
# 需要 pytest >= 7。
pythonpath = .
python_files = test_*.py
