# CPU 低功耗模式部署

设置页中的“CPU 低功耗模式”只在推理设备为 CPU 时生效。该预设使用本地 YOLO11n OpenVINO INT8 模型、`imgsz=512` 和每 4 帧一次检测；中间帧由 ByteTrack 预测轨迹保持目标框与区域计时。

## 安装运行时

桌面程序运行环境需要 OpenVINO：

```powershell
python -m pip install -r requirements-low-power.txt
```

离线导出推荐使用隔离环境，避免影响既有 PyTorch/CUDA 环境：

```powershell
python -m venv --system-site-packages .venv-openvino
.\.venv-openvino\Scripts\python.exe -m pip install -r requirements-low-power.txt
```

## 生成 INT8 模型

没有标注数据集时，可从代表性摄像头视频均匀抽取画面做激活量化校准：

```powershell
.\.venv-openvino\Scripts\python.exe tools\build_calibration_set.py `
  --video "8月11日-1.mp4" `
  --output artifacts\calibration\yolo11n-1080p-v1 `
  --count 128

.\.venv-openvino\Scripts\python.exe tools\export_openvino_int8.py `
  --weights yolo11n.pt `
  --data artifacts\calibration\yolo11n-1080p-v1\calibration.yaml `
  --output models\yolo11n_int8_openvino_model
```

模型目录需要包含：

```text
models/yolo11n_int8_openvino_model/
  yolo11n.xml
  yolo11n.bin
  model_manifest.json
```

抽样画面可生成可运行的 INT8 模型并用于性能/CPU 占用对比，但没有人工标注数据时，不能作为量化后准确率、漏报率或报警等价性的证明。

## 对比普通 CPU 模式

基准程序不启动 Qt、不写事件和截图、不播放声音，只比较视频解码、检测和跟踪：

```powershell
.\.venv-openvino\Scripts\python.exe tools\benchmark_cpu_profiles.py `
  --video "8月11日-1.mp4" `
  --trials 3
```

每次运行会在 `benchmarks/<时间戳>/` 写入 JSON 和 CSV，包含帧率、检测次数、进程 CPU 时间、任务管理器等价 CPU 占比、系统 CPU 占比、内存、线程数和时延。未安装硬件能耗计时，结果不包含焦耳或瓦特。

模型目录、清单或 OpenVINO 运行时缺失时，程序会在启动检测前显示具体原因；关闭该预设即可继续使用原有 PyTorch CPU 或 CUDA 推理路径。
